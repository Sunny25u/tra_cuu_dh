const form=document.getElementById('searchForm'), input=document.getElementById('phone'), result=document.getElementById('result');let orders=[];const norm=v=>{let s=(v||'').replace(/\D/g,'');if(s.startsWith('84')&&s.length===11)s='0'+s.slice(2);return s};const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));fetch('data/orders.json').then(r=>r.json()).then(d=>orders=d).catch(()=>{});function render(o){const items=o.items.length?o.items.map(i=>`<div class="item"><span>${esc(i.name)}</span><span class="qty">× ${i.qty}</span></div>`).join(''):'<div class="value">Chưa có thông tin sản phẩm</div>';result.innerHTML=`<article class="card"><div class="card-head"><div class="label">Đơn hàng của bạn</div><div class="name">${esc(o.name||'Khách hàng')}</div><div class="phone">${esc(o.phone)}</div></div><div class="rows"><div class="row"><div class="ico">📍</div><div class="row-body"><div class="row-title">Địa chỉ nhận hàng</div><div class="value">${esc(o.address||'Chưa cập nhật')}</div></div></div><div class="row"><div class="ico">🛍️</div><div class="row-body"><div class="row-title">Hàng gồm</div><div class="items">${items}</div></div></div><div class="row"><div class="ico">📦</div><div class="row-body"><div class="row-title">Mã vận đơn</div><div class="value">${o.tracking?esc(o.tracking):'<span class="pending">Chưa cập nhật</span>'}</div></div></div><div class="row"><div class="ico">🚚</div><div class="row-body"><div class="row-title">Phí ship phải trả</div><div class="value">${o.shippingFee!=null?new Intl.NumberFormat('vi-VN').format(o.shippingFee)+'đ':'<span class="pending">Chưa cập nhật</span>'}</div></div></div></div></article>`}function search(){const p=norm(input.value);if(p.length<9){result.innerHTML='<div class="empty"><div class="big">⌕</div><h2>Nhập số điện thoại</h2><p>Vui lòng nhập số điện thoại nhận hàng để tra cứu.</p></div>';return}const o=orders.find(x=>norm(x.phone)===p);if(!o){result.innerHTML='<div class="empty"><div class="big">♡</div><h2>Chưa tìm thấy đơn</h2><p>Kiểm tra lại số điện thoại hoặc thử nhập lại không có khoảng trắng.</p></div>';return}render(o)}form.addEventListener('submit',e=>{e.preventDefault();search()});input.addEventListener('input',()=>{input.value=input.value.replace(/[^0-9 +()-]/g,'')});